[project]
name = "adk-demo"
version = "0.1.0"
description = "Demo of deploying a Google ADK agent to LangSmith Deployments."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "deployments-wrap-sdk[google-adk]>=0.0.1",
    "langgraph-cli[inmem]>=0.4.27",
]
